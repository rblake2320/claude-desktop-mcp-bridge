# ──────────────────────────────────────────────────────────────
# skills-bridge MCP server — environment configuration
#
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# The deploy-vps.sh script generates this automatically.
# ──────────────────────────────────────────────────────────────

# Server binding
PORT=3001
HOST=127.0.0.1

# Authentication token — used as Bearer token in Authorization header.
# Generate one with:  openssl rand -hex 32
MCP_AUTH_TOKEN=your-secret-token-here

# Path to Claude skills directory
SKILLS_PATH=~/.claude/skills/

# Request timeout in milliseconds
TIMEOUT=60000

# Comma-separated list of allowed filesystem paths the server may access
ALLOWED_PATHS=/opt/skills-bridge

# Runtime environment: production | development
NODE_ENV=production
